{
  "name": "sanctions-screening-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@notionhq/workers": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
